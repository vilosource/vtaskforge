vtaskforge has been deployed to namespace {{ .Release.Namespace }}.

{{- if .Values.ingress.enabled }}

Access the API at:
  https://{{ .Values.ingress.apiHost }}

{{- if and .Values.mcp.enabled .Values.ingress.mcpHost }}
Access the MCP server at:
  https://{{ .Values.ingress.mcpHost }}
{{- end }}

{{- else }}

No ingress configured. Use port-forward to access:

  kubectl port-forward svc/{{ include "vtf.fullname" . }}-api {{ .Values.api.port }}:{{ .Values.api.port }} -n {{ .Release.Namespace }}

Then open: http://localhost:{{ .Values.api.port }}

{{- if .Values.mcp.enabled }}

For MCP access:

  kubectl port-forward svc/{{ include "vtf.fullname" . }}-mcp {{ .Values.mcp.port }}:{{ .Values.mcp.port }} -n {{ .Release.Namespace }}

{{- end }}
{{- end }}

{{- if .Values.migrate.superuser.enabled }}

Default login: {{ .Values.migrate.superuser.username }} / {{ .Values.migrate.superuser.password }}
Change these credentials after first login.
{{- end }}
