# Phase 7 — Task Detail Full Page View

Replace the cramped task detail modal with a full-page view that properly
renders the rich spec content. Parse the YAML spec into structured sections
(implementation approach, constraints, files, references) and render them
as proper UI elements with markdown support.

The Kanban board modal remains for quick-glance interactions, with a link
to the full page view for deep review.
